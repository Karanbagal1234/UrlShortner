<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Redirecting...</title>
</head>
<body>
    <script>
        // Retrieve the URL from the server-side variable
        const url = "<%= OrignalUrl %>";
        
     
        // Redirect to the URL
        if (url) {
            
            window.location.href = url;
        } else {
            console.error("No URL provided for redirection.");
        }
    </script>
</body>
</html>
